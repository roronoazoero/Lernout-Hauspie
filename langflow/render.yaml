services:
  - type: web
    name: langflow-agent
    env: docker
    region: frankfurt
    plan: free
    dockerfilePath: langflow/Dockerfile
    buildCommand: ""
    startCommand: ""
    autoDeploy: true
